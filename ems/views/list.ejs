<% if (users.length) { %>
  <table>
    <tr>
      <th>&nbsp;</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Position</th>
      <th>Start Date</th>
      <th>Shift</th>
    </tr>
    <% for (var index = 0; index < users.length; index++) { %>
      <tr>
        <td><img src="/images/<%= users[index].firstName %>_<%= users[index].lastName %>_th.jpg"
            alt="<%= users[index].firstName %> <%= users[index].lastName %> Image"></td>
        <td><%= users[index].firstName %></td>
        <td><%= users[index].lastName %></td>
        <td><%= users[index].position %></td>
        <td><%= users[index].startDate %></td>
        <td><%= users[index].shift %></td>
      </tr>
    <% } %>
  </table>
<% } %>
